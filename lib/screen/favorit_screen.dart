import 'package:flutter/material.dart';

class Favorit extends StatelessWidget {
   Favorit({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Text("Favorit",style: TextStyle(
        color: Colors.teal
    ),);
  }
}
